using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lesson4 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        float mag;
        mag = Vector3.right.magnitude;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
